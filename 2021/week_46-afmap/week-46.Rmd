---
title: "week-46"
author: "Maxwel Coura Oliveira"
date: "11/8/2021"
output: html_document
---



```{r}
#remotes::install_github("afrimapr/afrilearndata")
#remotes::install_github("afrimapr/afrihealthsites")
```

```{r}
library(afrilearndata)
library(afrihealthsites)
library(sf)
library(tidyverse)
library(ggtext)
```

```{r}
afrilearndata::afripop2020 
```

```{r}
afrihealthsites::compare_hs_sources("Kenya")
```

```{r}
world <- ne_countries(scale = "medium", returnclass = "sf")
class(world)
```

```{r}
world |> 
  arrange(sovereignt) |> 
  filter(region_un %in% "Africa") -> af_cont
```

```{r}
afrilearndata::afrihighway -> af_roads
afrilearndata::africontinent -> af
afrilearndata::africapitals -> af_capitals
```

  geom_text_repel(data = af_capitals,
                  aes(x=X, y=Y, label = capitalname),
            size = 6, 
            seed = 165,
            color = "beige", 
            fontface = "bold")

```{r}
af |> 
  ggplot() +
  geom_sf(color = "gray90") +
  geom_sf(data = af_roads, color = 2) +
  geom_sf(data = af_capitals) +
  coord_sf(xlim = c(-23, 60), 
           ylim = c(-40, 40), 
           expand = FALSE) +
  theme(panel.background = element_rect(fill = "aliceblue"),
        legend.position = c(0.80, 0.95),
        legend.background = element_rect(fill = NA),
        legend.direction = "horizontal",
        axis.text = element_markdown(size = 12),
        legend.title = element_markdown(face = "bold"),
        legend.key.width = unit(2,"line")) +
  annotation_scale(location = "br", 
                   width_hint = 0.4, 
                   text_cex = 1) + 
  annotation_north_arrow(location = "bl", 
                         which_north = "true",
                         pad_x = unit(0.05, "in"),
                         pad_y = unit(0.05, "in"), 
                         style = north_arrow_nautical)
```

